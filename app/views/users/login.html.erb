<%= form_for :user, url: logresult_path do |f| %>

	<%= f.label :email %>
	<%= f.text_field :email %><br />

	<%= f.label :password %>
	<%= f.text_field :password %><br />

	<%= f.submit "Save" %>

<% end %>